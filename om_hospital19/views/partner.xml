<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Partner Tree View - Phone before Email -->
    <record id="view_partner_tree_phone_before_email" model="ir.ui.view">
        <field name="name">res.partner.tree.phone.before.email</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">

            <!-- Move Phone before Email -->
            <xpath expr="//field[@name='email']" position="before">
                <field name="phone" position="move"/>
            </xpath>
        </field>
    </record>
</odoo>